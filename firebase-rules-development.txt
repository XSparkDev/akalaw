rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - Allow all operations for testing
    // TODO: Secure these rules for production
    
    // Payment records - allow all operations during development
    match /payments/{document} {
      allow read, write: if true;
    }
    
    // Customer records - allow all operations during development
    match /customers/{document} {
      allow read, write: if true;
    }
    
    // Download records - allow all operations during development
    match /downloads/{document} {
      allow read, write: if true;
    }
    
    // Analytics - allow all operations during development
    match /analytics/{document} {
      allow read, write: if true;
    }
    
    // Error logs - allow all operations during development
    match /payment_errors/{document} {
      allow read, write: if true;
    }
  }
} 